name: Release

on:
  push:
    tags:
      - "**"

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build and push the Docker image
      run: make docker-push TAG=${{ github.ref }}